**What changed, in one sentence?**

**Which track?** (scenario trace / policy variant within v0.1 / adapter & fixture / analysis)

**Test added or updated?** (a behavior change without a test will be asked for one)

**Determinism**: no clocks, no randomness, no floating point added in governance paths — confirm.

```bash
cargo fmt && cargo test --all-targets
```
